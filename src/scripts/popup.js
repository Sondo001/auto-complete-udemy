document.addEventListener('DOMContentLoaded', () => {
    document.querySelector('.app-name').textContent = chrome.i18n.getMessage('app_name');
    document.querySelector('.hope').textContent = chrome.i18n.getMessage('hope');
    document.querySelector('.rate-us').textContent = chrome.i18n.getMessage('rate_us')
}); // This is just a sample script. Paste your real code (javascript or HTML) here.

if ('this_is' == /an_example/) {
    of_beautifier();
} else {
    var a = b ? (c % d) : e[f];
}